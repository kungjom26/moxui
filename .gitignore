# Rust / Cargo
/target/
**/*.rs.bk
Cargo.lock.bak

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Secrets (NEVER commit)
.env
.env.local
.env.production
*.pem
*.key
*.crt
config.local.yaml
config.production.yaml

# Runtime data
data/
*.db
*.db-journal
*.db-wal
*.db-shm
backups/

# Build artifacts
*.tar.gz
*.zip

# Logs
*.log
logs/

# Coverage
coverage/
*.profraw

# Docker
Dockerfile.local
docker-compose.override.yml

# Hermes profiles (LOCAL-ONLY — never push to GitHub)
# Team uses 3 profiles: moxui-pm, moxui-coder, moxui-reviewer
# Located at ~/.hermes/profiles/moxui-*/
# NOT part of the project repo (intentional separation)
/.hermes/
hermes/
profiles/moxui-*/
**/moxui-pm/
**/moxui-coder/
**/moxui-reviewer/
**/SOUL.md
**/profile.yaml

# Team docs (LOCAL-ONLY — use ~/.hermes/shared/moxui/ for team coordination instead)
docs/TEAM.mdmoxui.db.audit
*.db.audit
